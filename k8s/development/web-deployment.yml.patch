spec:
  template:
    spec:
      containers:
      - name: frontend
        volumeMounts:
          - name: public
            readOnly: true
            mountPath: /usr/share/nginx/html
      volumes:
        - name: public
          persistentVolumeClaim:
            claimName: frontend-pvc
